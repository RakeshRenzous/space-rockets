import React from "react";
import {
  Flex,
  Alert,
  AlertIcon,
  SimpleGrid,
  AlertDescription,
} from "@chakra-ui/react";

export default function GridContainer({ data }) {
  return (
    <p></p>
  );
}
